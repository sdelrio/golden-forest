# /list-chars
List and summarize Fantasy Grounds character XML files.

## Instructions
1. List all `.xml` files in `static/fg/chars/`.
2. For each file, extract the character's `<name>` and `<race>` using `grep` or `read`.
3. Present a summary table to the user.
